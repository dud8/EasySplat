diff --git a/core/metal/bindings.h b/core/metal/bindings.h
index 51b2f931d427394934c3a43b31cd62936ad8c417..926ab439047ff98d256ca4513cdf630064a6e3f0 100644
--- a/core/metal/bindings.h
+++ b/core/metal/bindings.h
@@ -93,6 +93,11 @@ void msplat_set_raster_memory_budget_and_fail_for_testing(uint64_t budget_bytes)
 void msplat_fail_next_sync_for_testing();
 uint64_t msplat_pending_exact_raster_timing_handlers_for_testing();
 uint32_t msplat_exact_radix_pass_count_for_testing(uint32_t tile_count);
+void msplat_exact_prefix_sum_for_testing(
+    const int32_t* input,
+    uint32_t count,
+    uint64_t* output
+);
 void msplat_exact_radix_sort_for_testing(
     const uint64_t* keys,
     const uint32_t* values,
@@ -128,6 +133,20 @@ void msplat_copy_last_raster_debug(
     float* opacity_gradients,
     int point_count
 );
+void msplat_copy_last_raster_reference_debug(
+    float* xys,
+    float* depths,
+    int* radii,
+    float* aabb,
+    float* conics,
+    float* raw_colors,
+    float* render_gradients,
+    float* projected_position_gradients,
+    float* raster_color_gradients,
+    float* opacity_gradients,
+    int point_count,
+    int pixel_count
+);
 #endif
 MsplatRasterStats msplat_get_raster_stats();
 bool msplat_raster_memory_budget_was_exceeded();
diff --git a/core/metal/msplat_metal.metal b/core/metal/msplat_metal.metal
index d80b1c14a86f425d54b68fa7c4dfe6aeff992d36..75de08c650699a4293a25e05302f8c383a5b8ff4 100644
--- a/core/metal/msplat_metal.metal
+++ b/core/metal/msplat_metal.metal
@@ -1004,6 +1004,22 @@ kernel void exact_block_reduce_u64_kernel(
     if (lane == 0) block_totals[group] = values[0];
 }
 
+kernel void exact_block_offsets_u64_kernel(
+    constant uint& count [[buffer(0)]],
+    device uint64_t* block_totals [[buffer(1)]],
+    device atomic_uint* overflow_flag [[buffer(2)]],
+    uint index [[thread_position_in_grid]]
+) {
+    if (index != 0 || atomic_load_explicit(overflow_flag, memory_order_relaxed) == 0u) return;
+    const uint block_count = (count + EXACT_PREFIX_TG_SIZE - 1) / EXACT_PREFIX_TG_SIZE;
+    uint64_t running = 0;
+    for (uint block = 0; block < block_count; ++block) {
+        const uint64_t total = block_totals[block];
+        block_totals[block] = running;
+        running += total;
+    }
+}
+
 kernel void exact_block_scan_propagate_u64_kernel(
     constant uint& count [[buffer(0)]],
     device const int32_t* input [[buffer(1)]],
@@ -1018,16 +1034,13 @@ kernel void exact_block_scan_propagate_u64_kernel(
     uint index = group * EXACT_PREFIX_TG_SIZE + lane;
     inclusive[lane] = index < count ? (uint64_t)max(input[index], 0) : 0u;
     threadgroup_barrier(mem_flags::mem_threadgroup);
-    if (lane == 0) {
-        uint64_t running = 0;
-        for (uint prior = 0; prior < group; ++prior) running += block_totals[prior];
-        for (uint offset = 0; offset < EXACT_PREFIX_TG_SIZE; ++offset) {
-            running += inclusive[offset];
-            inclusive[offset] = running;
-        }
+    for (uint offset = 1; offset < EXACT_PREFIX_TG_SIZE; offset <<= 1) {
+        const uint64_t addend = lane >= offset ? inclusive[lane - offset] : 0u;
+        threadgroup_barrier(mem_flags::mem_threadgroup);
+        inclusive[lane] += addend;
+        threadgroup_barrier(mem_flags::mem_threadgroup);
     }
-    threadgroup_barrier(mem_flags::mem_threadgroup);
-    if (index < count) output[index] = inclusive[lane];
+    if (index < count) output[index] = block_totals[group] + inclusive[lane];
 }
 
 kernel void prepare_exact_raster_kernel(
diff --git a/core/metal/msplat_metal.mm b/core/metal/msplat_metal.mm
index a9fdd8ff54162221920dc27daf5b2a54a8e1683d..037f3d2e89ab2f5505be309c2a6984d3cd0c3ac0 100644
--- a/core/metal/msplat_metal.mm
+++ b/core/metal/msplat_metal.mm
@@ -16,6 +16,7 @@
 #import <array>
 #import <mutex>
 #include <cmath>
+#include <cstdio>
 #include <cstring>
 #include <limits>
 #include <stdexcept>
@@ -211,6 +212,10 @@ struct MetalContext {
         }
         return _currentCB;
     }
+    id<MTLCommandBuffer> getRootCommandBuffer() {
+        (void)getCommandBuffer();
+        return _currentCB.rootCommandBuffer;
+    }
     void commitCB() {
         if (_currentCB) {
             if (g_gpu_timing_enabled) {
@@ -320,6 +325,7 @@ struct MetalContext {
     id<MTLComputePipelineState> clear_exact_tile_bins_kernel_cpso;
     id<MTLComputePipelineState> count_exact_tile_intersections_kernel_cpso;
     id<MTLComputePipelineState> exact_block_reduce_u64_kernel_cpso;
+    id<MTLComputePipelineState> exact_block_offsets_u64_kernel_cpso;
     id<MTLComputePipelineState> exact_block_scan_propagate_u64_kernel_cpso;
     id<MTLComputePipelineState> map_gaussian_to_intersects_kernel_cpso;
     id<MTLComputePipelineState> radix_sort_histogram_kernel_cpso;
@@ -460,6 +466,7 @@ MetalContext* init_msplat_metal_context() {
     ctx->count_exact_tile_intersections_kernel_cpso =
         load(@"count_exact_tile_intersections_kernel");
     ctx->exact_block_reduce_u64_kernel_cpso       = load(@"exact_block_reduce_u64_kernel");
+    ctx->exact_block_offsets_u64_kernel_cpso      = load(@"exact_block_offsets_u64_kernel");
     ctx->exact_block_scan_propagate_u64_kernel_cpso =
         load(@"exact_block_scan_propagate_u64_kernel");
     ctx->map_gaussian_to_intersects_kernel_cpso   = load(@"map_gaussian_to_intersects_kernel");
@@ -725,6 +732,8 @@ static std::mutex g_exact_raster_metric_mutex;
 static uint64_t g_pending_exact_raster_timing_handlers = 0;
 static double g_exact_fallback_elapsed_seconds = 0;
 static bool g_exact_raster_timing_invalid = false;
+static std::string g_exact_raster_timing_failure;
+static bool g_exact_raster_timing_warning_emitted = false;
 static uint64_t g_exact_buffer_growth_count = 0;
 static uint64_t g_exact_buffer_bytes_added = 0;
 static uint64_t g_peak_exact_intersection_capacity = 0;
@@ -1184,18 +1193,47 @@ static void attach_exact_raster_timing_handler() {
         ++g_pending_exact_raster_timing_handlers;
     }
     timingFlags[slot] = 0;
-    id<MTLCommandBuffer> commandBuffer = get_global_context()->getCommandBuffer();
+    const uint64_t iteration = g_raster_iteration;
+    const uint64_t cameraIndex = g_raster_camera_index;
+    id<MTLCommandBuffer> commandBuffer = get_global_context()->getRootCommandBuffer();
     dispatch_group_enter(g_exact_raster_timing_group);
     [commandBuffer addCompletedHandler:^(id<MTLCommandBuffer> completedCommandBuffer) {
         {
             std::lock_guard<std::mutex> lock(g_exact_raster_metric_mutex);
-            if (timingFlags[slot] != 0) {
+            const MTLCommandBufferStatus status = completedCommandBuffer.status;
+            if (status != MTLCommandBufferStatusCompleted) {
+                const char *description = completedCommandBuffer.error
+                    ? [[completedCommandBuffer.error description] UTF8String]
+                    : "unknown Metal command-buffer error";
+                g_exact_raster_timing_invalid = true;
+                if (g_exact_raster_timing_failure.empty()) {
+                    g_exact_raster_timing_failure =
+                        "msplat: Metal command buffer failed while exact-raster tracking was active";
+                    g_exact_raster_timing_failure +=
+                        " iteration=" + std::to_string(iteration) +
+                        " camera=" + std::to_string(cameraIndex) +
+                        " status=" + std::to_string(static_cast<long long>(status)) +
+                        " error=" + (description ? description : "unknown Metal command-buffer error");
+                }
+            } else if (timingFlags[slot] != 0) {
                 const double started = completedCommandBuffer.GPUStartTime;
                 const double ended = completedCommandBuffer.GPUEndTime;
                 if (std::isfinite(started) && std::isfinite(ended) && ended > started) {
                     g_exact_fallback_elapsed_seconds += ended - started;
-                } else {
-                    g_exact_raster_timing_invalid = true;
+                } else if (!g_exact_raster_timing_warning_emitted) {
+                    std::fprintf(
+                        stderr,
+                        "msplat: warning event=exact_raster_gpu_timing_unavailable "
+                        "iteration=%llu camera=%llu status=%lld gpu_start=%.17g "
+                        "gpu_end=%.17g\n",
+                        static_cast<unsigned long long>(iteration),
+                        static_cast<unsigned long long>(cameraIndex),
+                        static_cast<long long>(status),
+                        started,
+                        ended
+                    );
+                    std::fflush(stderr);
+                    g_exact_raster_timing_warning_emitted = true;
                 }
             }
             --g_pending_exact_raster_timing_handlers;
@@ -1207,7 +1245,11 @@ static void attach_exact_raster_timing_handler() {
 static void throw_if_exact_raster_timing_invalid() {
     std::lock_guard<std::mutex> lock(g_exact_raster_metric_mutex);
     if (g_exact_raster_timing_invalid) {
-        throw std::runtime_error("msplat: exact raster GPU timing was unavailable");
+        throw std::runtime_error(
+            g_exact_raster_timing_failure.empty()
+                ? "msplat: exact raster GPU timing was unavailable"
+                : g_exact_raster_timing_failure
+        );
     }
 }
 
@@ -1250,6 +1292,74 @@ uint32_t msplat_exact_radix_pass_count_for_testing(uint32_t tile_count) {
     return exact_radix_pass_count(tile_count);
 }
 
+void msplat_exact_prefix_sum_for_testing(
+    const int32_t* input,
+    uint32_t count,
+    uint64_t* output
+) {
+    if (count == 0) return;
+    if (!input || !output) {
+        throw std::invalid_argument("msplat: exact prefix oracle received a null buffer");
+    }
+
+    MetalContext *context = get_global_context();
+    const uint32_t block_count =
+        count / 1024u + static_cast<uint32_t>(count % 1024u != 0);
+    MTensor device_input = mtensor_empty(
+        context->device,
+        {static_cast<int64_t>(count)},
+        DType::Int32
+    );
+    MTensor device_output = mtensor_empty(
+        context->device,
+        {static_cast<int64_t>(count)},
+        DType::Int64
+    );
+    MTensor block_totals = mtensor_empty(
+        context->device,
+        {static_cast<int64_t>(block_count)},
+        DType::Int64
+    );
+    MTensor overflow = mtensor_zeros(context->device, {1}, DType::Int32);
+    std::memcpy(
+        device_input.data<int32_t>(),
+        input,
+        static_cast<size_t>(count) * sizeof(*input)
+    );
+    overflow.data<uint32_t>()[0] = 1;
+
+    id<MTLCommandBuffer> command_buffer = context->getCommandBuffer();
+    id<MTLComputeCommandEncoder> encoder = [command_buffer computeCommandEncoder];
+    [encoder setComputePipelineState:context->exact_block_reduce_u64_kernel_cpso];
+    ENC_SCALAR(encoder, count, 0); ENC_BUF(encoder, device_input, 1);
+    ENC_BUF(encoder, block_totals, 2); ENC_BUF(encoder, overflow, 3);
+    [encoder dispatchThreadgroups:MTLSizeMake(block_count, 1, 1)
+                threadsPerThreadgroup:MTLSizeMake(1024, 1, 1)];
+    [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
+
+    [encoder setComputePipelineState:context->exact_block_offsets_u64_kernel_cpso];
+    ENC_SCALAR(encoder, count, 0); ENC_BUF(encoder, block_totals, 1);
+    ENC_BUF(encoder, overflow, 2);
+    [encoder dispatchThreads:MTLSizeMake(1, 1, 1)
+       threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
+    [encoder memoryBarrierWithScope:MTLBarrierScopeBuffers];
+
+    [encoder setComputePipelineState:context->exact_block_scan_propagate_u64_kernel_cpso];
+    ENC_SCALAR(encoder, count, 0); ENC_BUF(encoder, device_input, 1);
+    ENC_BUF(encoder, device_output, 2); ENC_BUF(encoder, block_totals, 3);
+    ENC_BUF(encoder, overflow, 4);
+    [encoder dispatchThreadgroups:MTLSizeMake(block_count, 1, 1)
+                threadsPerThreadgroup:MTLSizeMake(1024, 1, 1)];
+    [encoder endEncoding];
+    context->syncCB();
+
+    std::memcpy(
+        output,
+        device_output.data<uint64_t>(),
+        static_cast<size_t>(count) * sizeof(*output)
+    );
+}
+
 void msplat_exact_radix_sort_for_testing(
     const uint64_t* keys,
     const uint32_t* values,
@@ -1882,6 +1992,47 @@ void msplat_copy_last_raster_debug(
     std::memcpy(opacity_gradients, g_tcache.v_opacity.data<float>(),
                 static_cast<size_t>(point_count) * sizeof(float));
 }
+
+void msplat_copy_last_raster_reference_debug(
+    float* xys,
+    float* depths,
+    int* radii,
+    float* aabb,
+    float* conics,
+    float* raw_colors,
+    float* render_gradients,
+    float* projected_position_gradients,
+    float* raster_color_gradients,
+    float* opacity_gradients,
+    int point_count,
+    int pixel_count
+) {
+    if (xys == nullptr || depths == nullptr || radii == nullptr || aabb == nullptr ||
+        conics == nullptr || raw_colors == nullptr || render_gradients == nullptr ||
+        projected_position_gradients == nullptr || raster_color_gradients == nullptr ||
+        opacity_gradients == nullptr || point_count <= 0 || pixel_count <= 0 ||
+        g_tcache.xys.numel() != static_cast<int64_t>(point_count) * 2 ||
+        g_tcache.depths.numel() != point_count || g_tcache.radii_out.numel() != point_count ||
+        g_tcache.aabb.numel() != static_cast<int64_t>(point_count) * 2 ||
+        g_tcache.conics.numel() != static_cast<int64_t>(point_count) * 3 ||
+        g_tcache.colors.numel() != static_cast<int64_t>(point_count) * 3 ||
+        g_tcache.v_rendered.numel() != static_cast<int64_t>(pixel_count) * 3 ||
+        g_tcache.v_xy.numel() != static_cast<int64_t>(point_count) * 2 ||
+        g_tcache.v_colors_rast.numel() != static_cast<int64_t>(point_count) * 3 ||
+        g_tcache.v_opacity.numel() != point_count) {
+        throw std::runtime_error("msplat: CPU raster reference snapshot dimensions are invalid");
+    }
+    std::memcpy(xys, g_tcache.xys.data<float>(), static_cast<size_t>(point_count) * 2 * sizeof(float));
+    std::memcpy(depths, g_tcache.depths.data<float>(), static_cast<size_t>(point_count) * sizeof(float));
+    std::memcpy(radii, g_tcache.radii_out.data<int>(), static_cast<size_t>(point_count) * sizeof(int));
+    std::memcpy(aabb, g_tcache.aabb.data<float>(), static_cast<size_t>(point_count) * 2 * sizeof(float));
+    std::memcpy(conics, g_tcache.conics.data<float>(), static_cast<size_t>(point_count) * 3 * sizeof(float));
+    std::memcpy(raw_colors, g_tcache.colors.data<float>(), static_cast<size_t>(point_count) * 3 * sizeof(float));
+    std::memcpy(render_gradients, g_tcache.v_rendered.data<float>(), static_cast<size_t>(pixel_count) * 3 * sizeof(float));
+    std::memcpy(projected_position_gradients, g_tcache.v_xy.data<float>(), static_cast<size_t>(point_count) * 2 * sizeof(float));
+    std::memcpy(raster_color_gradients, g_tcache.v_colors_rast.data<float>(), static_cast<size_t>(point_count) * 3 * sizeof(float));
+    std::memcpy(opacity_gradients, g_tcache.v_opacity.data<float>(), static_cast<size_t>(point_count) * sizeof(float));
+}
 #endif
 
 static void throw_if_raster_failed() {
@@ -1982,6 +2133,8 @@ void cleanup_msplat_metal() {
         g_pending_exact_raster_timing_handlers = 0;
         g_exact_fallback_elapsed_seconds = 0;
         g_exact_raster_timing_invalid = false;
+        g_exact_raster_timing_failure.clear();
+        g_exact_raster_timing_warning_emitted = false;
         g_exact_buffer_growth_count = 0;
         g_exact_buffer_bytes_added = 0;
         g_peak_exact_intersection_capacity = 0;
@@ -2058,6 +2211,12 @@ static void encode_exact_raster_fallback(
                           threadsPerThreadgroup:MTLSizeMake(1024, 1, 1)];
     [enc memoryBarrierWithScope:MTLBarrierScopeBuffers];
 
+    [enc setComputePipelineState:ctx->exact_block_offsets_u64_kernel_cpso];
+    ENC_SCALAR(enc, num_points, 0); ENC_BUF(enc, g_tcache.block_totals, 1);
+    ENC_BUF(enc, g_tcache.overflow_flag, 2);
+    [enc dispatchThreads:MTLSizeMake(1, 1, 1) threadsPerThreadgroup:MTLSizeMake(1, 1, 1)];
+    [enc memoryBarrierWithScope:MTLBarrierScopeBuffers];
+
     [enc setComputePipelineState:ctx->exact_block_scan_propagate_u64_kernel_cpso];
     ENC_SCALAR(enc, num_points, 0); ENC_BUF(enc, g_tcache.num_tiles_hit, 1);
     ENC_BUF(enc, g_tcache.cum_tiles_hit, 2); ENC_BUF(enc, g_tcache.block_totals, 3);
diff --git a/core/metal/msplat_metal.mm b/core/metal/msplat_metal.mm
--- a/core/metal/msplat_metal.mm
+++ b/core/metal/msplat_metal.mm
@@ -1532,6 +1532,8 @@ void msplat_sync_loss_window(int count, float* output) {
         if (!std::isfinite(value)) {
             throw std::runtime_error("msplat: loss window contains a non-finite value");
         }
-        output[index] = static_cast<float>(value);
+        // L1 + (1 - SSIM) is nonnegative. Discard only the floating-point
+        // undershoot in its checkpoint and early-stop evidence.
+        output[index] = static_cast<float>(std::max(0.0, value));
     }
 }
diff --git a/core/metal/msplat_metal.mm b/core/metal/msplat_metal.mm
--- a/core/metal/msplat_metal.mm
+++ b/core/metal/msplat_metal.mm
@@ -2578,32 +2578,38 @@ static void forward_pipeline(
     // Fused loss: ssim_h_fwd → fused_v_fwd_h_bwd → ssim_v_bwd
     // Eliminates loss_intermediates round-trip (130 MB/iter bandwidth saved).
     auto encode_loss_fwd_bwd = [&](id<MTLComputeCommandEncoder> enc) {
-        MTLSize grid = MTLSizeMake(img_width, img_height, 1);
         MTLSize tg = MTLSizeMake(8, 8, 1);
+        // Each SSIM stage cooperatively fills threadgroup memory with all 64
+        // threads. Dispatch complete groups and let the kernels mask edge pixels.
+        MTLSize loss_threadgroups = MTLSizeMake(
+            (static_cast<NSUInteger>(img_width) + tg.width - 1) / tg.width,
+            (static_cast<NSUInteger>(img_height) + tg.height - 1) / tg.height,
+            1
+        );
         // Pass 1: H conv on images → ssim_h_buf
         [enc setComputePipelineState:ctx->ssim_h_fwd_kernel_cpso];
         ENC_BUF(enc, out_img, 0); ENC_BUF(enc, gt, 1);
         [enc setBytes:loss_img_size->data() length:sizeof(*loss_img_size) atIndex:2];
         ENC_BUF(enc, g_tcache.ssim_h_buf, 3);
-        [enc dispatchThreads:grid threadsPerThreadgroup:tg];
+        [enc dispatchThreadgroups:loss_threadgroups threadsPerThreadgroup:tg];
         [enc memoryBarrierWithScope:MTLBarrierScopeBuffers];
         // Pass 2: Fused V fwd + H bwd
         [enc setComputePipelineState:ctx->ssim_fused_v_fwd_h_bwd_kernel_cpso];
         ENC_BUF(enc, out_img, 0); ENC_BUF(enc, gt, 1);
         ENC_BUF(enc, g_tcache.ssim_h_buf, 2);
         [enc setBytes:loss_img_size->data() length:sizeof(*loss_img_size) atIndex:3];
         ENC_SCALAR(enc, ssim_weight, 4); ENC_SCALAR(enc, loss_inv_n, 5);
         ENC_BUF(enc, loss_intermediates, 6); ENC_BUF(enc, loss_sum, 7);
-        [enc dispatchThreads:grid threadsPerThreadgroup:tg];
+        [enc dispatchThreadgroups:loss_threadgroups threadsPerThreadgroup:tg];
         [enc memoryBarrierWithScope:MTLBarrierScopeBuffers];
         // Pass 3: V bwd
         [enc setComputePipelineState:ctx->ssim_v_bwd_kernel_cpso];
         ENC_BUF(enc, out_img, 0); ENC_BUF(enc, gt, 1);
         ENC_BUF(enc, loss_intermediates, 2);
         [enc setBytes:loss_img_size->data() length:sizeof(*loss_img_size) atIndex:3];
         ENC_SCALAR(enc, ssim_weight, 4); ENC_SCALAR(enc, loss_inv_n, 5);
         ENC_BUF(enc, v_rendered, 6);
-        [enc dispatchThreads:grid threadsPerThreadgroup:tg];
+        [enc dispatchThreadgroups:loss_threadgroups threadsPerThreadgroup:tg];
     };
 
     auto encode_rast_bwd = [&](id<MTLComputeCommandEncoder> enc) {
diff --git a/core/metal/msplat_metal.metal b/core/metal/msplat_metal.metal
--- a/core/metal/msplat_metal.metal
+++ b/core/metal/msplat_metal.metal
@@ -4110,6 +4110,7 @@ kernel void ssim_fused_v_fwd_h_bwd_kernel(
     constexpr uint TILE_DIM = SSIM_TG + 2 * SSIM_HALF_WIN;
     constexpr uint TILE_PIXELS = TILE_DIM * TILE_DIM;
     float ssim_sum = 0.0f, l1_sum = 0.0f;
+    uint valid_pixel_count = 0;
 
     for (uint c = 0; c < 3; c++) {
         threadgroup float tg_hp[TILE_DIM][TILE_DIM][5];
@@ -4146,13 +4147,22 @@ kernel void ssim_fused_v_fwd_h_bwd_kernel(
             float iCD = 1.0f / (Cd * D);
             float dmu = 2.0f*B*(mu_x*Cd - A*mu_y) / (Cd*Cd*D);
             float dsyq = -A*B*iCD/D, dsxy = 2.0f*A*iCD;
-            tg_f1[dy][dx] = dmu - 2.0f*mu_y*dsyq - mu_x*dsxy;
-            tg_f2[dy][dx] = 2.0f*dsyq;
-            tg_f3[dy][dx] = dsxy;
-            if (dx >= SSIM_HALF_WIN && dx < SSIM_HALF_WIN + SSIM_TG) {
-                int gpx = base_gx + (int)dx, gpy = base_gy + (int)(dy + SSIM_HALF_WIN);
-                if (gpx >= 0 && gpx < (int)W && gpy >= 0 && gpy < (int)H) {
-                    ssim_sum += (A * B) / (Cd * D);
-                    l1_sum += fabs(gt[(gpy*W+gpx)*3+c] - rendered[(gpy*W+gpx)*3+c]);
-                }
+            const int gpx = base_gx + (int)dx;
+            const int gpy = base_gy + (int)(dy + SSIM_HALF_WIN);
+            const bool valid_center =
+                gpx >= 0 && gpx < (int)W && gpy >= 0 && gpy < (int)H;
+            if (valid_center) {
+                tg_f1[dy][dx] = dmu - 2.0f*mu_y*dsyq - mu_x*dsxy;
+                tg_f2[dy][dx] = 2.0f*dsyq;
+                tg_f3[dy][dx] = dsxy;
+            } else {
+                tg_f1[dy][dx] = 0.0f;
+                tg_f2[dy][dx] = 0.0f;
+                tg_f3[dy][dx] = 0.0f;
+            }
+            if (valid_center &&
+                dx >= SSIM_HALF_WIN && dx < SSIM_HALF_WIN + SSIM_TG) {
+                if (c == 0) ++valid_pixel_count;
+                ssim_sum += (A * B) / (Cd * D);
+                l1_sum += fabs(gt[(gpy*W+gpx)*3+c] - rendered[(gpy*W+gpx)*3+c]);
             }
@@ -4172,7 +4182,9 @@ kernel void ssim_fused_v_fwd_h_bwd_kernel(
         threadgroup_barrier(mem_flags::mem_threadgroup);
     }
 
-    float pixel_loss = (px < W && py < H) ? ssim_weight*(1.0f-ssim_sum/3.0f) + (1.0f-ssim_weight)*l1_sum/3.0f : 0.0f;
+    float pixel_loss =
+        ssim_weight * (float(valid_pixel_count) - ssim_sum / 3.0f) +
+        (1.0f - ssim_weight) * l1_sum / 3.0f;
     threadgroup float tg_sum[256];
     tg_sum[tr] = pixel_loss;
     threadgroup_barrier(mem_flags::mem_threadgroup);
