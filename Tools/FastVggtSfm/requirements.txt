# Runtime deps are primarily satisfied by the toolchain's bundled CPython + vendored FastVGGT.
# This file documents the minimum imports used by EasySplat's FastVGGT bridge.
# Keep the PyTorch pair pinned for reproducible Apple Silicon toolchain builds.
# As of 2026-03-13, torchvision 0.25.0 explicitly requires torch 2.10.0 on PyPI.
torch==2.10.0
torchvision==0.25.0
numpy
Pillow
huggingface_hub
einops
safetensors
